CREATE TABLE User(
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT NOT NULL
);

selectAll:
SELECT * FROM User;

insertUser:
INSERT INTO User(id, name, email)
VALUES (?, ?, ?);

deleteAll:
DELETE FROM User;